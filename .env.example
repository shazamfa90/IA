# Canal padrão da mesa: o app já abre apontando pra ele, ninguém precisa digitar.
# Copie este arquivo para .env.local (que é ignorado pelo git) e ponha a URL real.
# Em produção o valor vem do secret DISCORD_WEBHOOK, injetado no build pelo workflow.
VITE_WEBHOOK_URL=https://discord.com/api/webhooks/SEU_ID/SEU_TOKEN
